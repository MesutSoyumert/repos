using System;

namespace Ders2
{
    class Program
    {
        static void Main(string[] args)
        {
            // Sayı tutan değişkenler
            uint deger; // Pozitif tam sayılar
            int deger2; // Tam sayıları tutar (- ve + sayılar)
            double deger3=5.2;
            float deger4=1.8f;
            long deger5;
            ulong deger6;
            short deger7 = 12;
            ushort deger8 = 12;

            // =======================

            // Yazı Tipi tutan değişkenler
            string yazi = "Merhaba Konsol!";
            string yazi1 = "3487589593";
            char klavye_a = 'a'; // Tek hane karakter
            bool kosul = true; // Örneğin .. tuşuna bastı ise true değeri döner
            bool kosul1 = false;

            int a = 5;
            int b = 9;
            bool kosul2 = a < b;
            if (a < b) ;
            if (kosul2) ;
            // ===========================

        }
    }
}
